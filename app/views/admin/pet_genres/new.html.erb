<h1>ペットジャンル追加</h1>

<%= form_with model: @pet_genre, url: admin_pet_genres_path do |f| %>
  <p>ジャンル名</p>
  <%= f.text_field :name %>

  <div>
    <%= f.submit '追加', class:'btn btn-success' %>
  </div>
<% end %>