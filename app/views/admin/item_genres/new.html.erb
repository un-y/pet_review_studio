<%= render 'admin/shared/list', items: @new_admin_item_genres_path %>

<h1>商品ジャンル追加</h1>

<%= form_with model: @item_genre, url: admin_item_genres_path do |f| %>
  <p>ジャンル名</p>
  <%= f.text_field :name %>

  <div>
    <%= f.submit '追加', class:'btn btn-success' %>
  </div>
<% end %>