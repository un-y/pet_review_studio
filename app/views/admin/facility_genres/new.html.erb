<%= render 'admin/shared/list', items: @new_admin_facility_genres_path %>

<h1>施設ジャンル追加</h1>

<%= form_with model: @facility_genre, url: admin_facility_genres_path do |f| %>
  <p>ジャンル名</p>
  <%= f.text_field :name %>

  <div>
    <%= f.submit '追加', class:'btn btn-success' %>
  </div>
<% end %>